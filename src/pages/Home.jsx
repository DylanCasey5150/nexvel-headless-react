export default function Home() {
  return (
    <div className="page" style={{ display: "grid", gap: 12 }}>
      <h1>Build Next-Level Web Experiences</h1>
      <p className="lead">
        A headless WordPress + React demo: content managed in WordPress (CPT + ACF) and rendered in a fast React UI.
      </p>
    </div>
  );
}